import 'package:camera_studio/filters/filters.dart';

List<Filter> filters = [
  const NoFilter(),
  const RedFilter(),
  const GreenFilter(),
  const BlueFilter(),
  const NeonFilter(),
  const SepiaFilter(),
  const GrayscaleFilter(),
  const InvertFilter(),
  const VintageFilter(),
];
